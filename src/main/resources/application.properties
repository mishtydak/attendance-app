


# Give your application a name
spring.application.name=attendance-app

# --- Database Configuration for Production (Cloud) ---
spring.datasource.url=${jdbc:postgresql://db.iwsdziaxsazryzidosmd.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${postgres}
spring.datasource.password=${tanishkdak136}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA/Hibernate Configuration for PostgreSQL ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- H2 Console is disabled in production ---
spring.h2.console.enabled=false

# *****************************************************************
# *** THIS IS THE NEW, CRITICAL DIAGNOSTIC LOGGING SETTING ***
# *****************************************************************
# This forces Hibernate and the database driver to print detailed logs.
logging.level.org.hibernate=DEBUG
# *****************************************************************